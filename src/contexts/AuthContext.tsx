
// This file is now only used to re-export from the new location
// to maintain backward compatibility
export { AuthContext, useAuth, AuthProvider } from './auth';
export type { AuthContextType } from './auth';
